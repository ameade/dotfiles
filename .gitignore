caches/
backups/
__pycache__/

# Secrets / machine-specific config — never commit these.
*.local
.bashrc.local
.bash_profile.local
source/*.local.sh
claude/settings.local.json

# SSH material, in case anyone drops keys under link/.ssh
link/.ssh/authorized_keys
link/.ssh/id_*
link/.ssh/known_hosts

*LOCAL*
